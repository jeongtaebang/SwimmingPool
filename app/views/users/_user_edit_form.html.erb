<div class="login-box">
  <h2>회원정보 수정</h2>

  <%= form_for(@user, url: user_path(@user), html: { method: :put }) do |f| %>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true %>
    </div>

    <div class="field">
      <%= f.label :name, "이름" %><br />
      <%= f.text_field :name, autofocus: true %>
   </div>

    <% if !(@user.confirmed?) %>
      <div>Currently waiting confirmation for: <%= @user.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :password, "새 비밀번호" %><br />
      <%= f.password_field :password, autocomplete: "off", placeholder: "수정 할 경우에만 입력" %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation, "새 비밀번호 확인" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>

   <div class="field">
     <%= f.label :current_password, "현재 비밀번호" %><br />
     <%= f.password_field :current_password, autocomplete: "off", placeholder: "수정 확인을 위해 입력" %>
   </div>

    <div class="actions">
     <%= f.submit "회원정보 수정" %>
   </div>
  <% end %>

  <hr>

  <h3>회원탈퇴</h3>

  <p>정말 탈퇴하시겠습니까? <%= button_to "회원탈퇴", registration_path(current_user), data: { confirm: "정말 탈퇴하시겠습니까??" },  method: :delete %></p>

  <%= link_to "뒤로", :back %>
</div>